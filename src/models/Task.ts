export interface Task {
    taskId: string;
    taskName: string;
    description: string;
    startTime: string;
    deadline: string;
    status: boolean;
    taskScore: string;
    submissionStatus: string;
    clubMemberId: string;
}